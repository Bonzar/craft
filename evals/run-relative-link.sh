#!/usr/bin/env bash
# Евал правила «Ссылка на файл — от рабочей директории или абсолютом».
#
# Regression-tests реальный инцидент: отчёт о правках в arc-маунте был собран
# markdown-ссылками с путями относительно /Users/dev/arc-mounts/, тогда как
# рабочая директория сессии — совсем другой репозиторий. Относительный путь
# резолвится от неё, поэтому ни одна ссылка у Влада не открылась.
#
# ЧТО ИМЕННО МЕРИТСЯ. Промптовый слой: держится ли правило само, до того как
# сработает машинная подпорка. Подпорка есть — Stop-хук
# .claude/hooks/universal-stop-relative-link.sh блокирует завершение хода с
# неразрешимой ссылкой, — но он срабатывает уже ПОСЛЕ показа сообщения, то есть
# битую ссылку пользователь всё равно увидит. Цена промаха ненулевая, поэтому
# правило меряется отдельно от хука.
#
# Улика доставки — сообщение SessionStart-инжекта роутера: правило живёт в
# системной зоне памяти, всегда-в-контексте, отдельного триггер-хука у него нет.
#
# Usage: run-relative-link.sh [model ...]     (default: claude-sonnet-5)
#        EVAL_RUNS=3 run-relative-link.sh …   — N прогонов каждого кейса
#
# Дисциплина прогонов и порог устойчивости — evals/README.md.
set -u
cd "$(dirname "$0")/.." || exit 1
source evals/lib/runner.sh

eval_run evals/cases/behavioral/relative-link.jsonl "$@"
