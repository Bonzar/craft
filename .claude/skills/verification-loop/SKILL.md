---
name: verification-loop
description: Чекпоинт-проверка перед объявлением «готово» и перед PR — build → typecheck → lint → тесты. Активируй, когда завершаешь фичу или правку и собираешься отчитаться («готово», «сделал», «done»), перед созданием PR («заведи PR», «запушь», «create PR»), после рефакторинга. «Написал код» ≠ «работает»: без прогона проверок отчёт о готовности не выдаётся.
---

# Шим: тело — в Craft

Тело скилла живёт в Craft-доке «⚙️ SKILL: Verification loop»
(block-ID `83ad829d-6fca-2ad3-d851-4181baa84620`). Прочитай его ЦЕЛИКОМ —
`blocks get 83ad829d-6fca-2ad3-d851-4181baa84620 --depth -1` через Craft MCP —
и действуй строго по нему.

Craft недоступен (ни MCP, ни connect) — ЯВНО скажи Владу, что тело скилла
недоступно, и не имитируй процедуру по памяти или усечённой версии.
