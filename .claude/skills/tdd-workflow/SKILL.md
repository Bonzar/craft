---
name: tdd-workflow
description: Дисциплина red/green/refactor при изменении поведения — падающая проверка до реализации. Активируй при новой фиче, багфиксе или рефакторинге логики («напиши фичу», «почини баг», «fix the bug», «добавь функцию», «отрефактори», «покрой тестами»), А ТАКЖЕ при правке правила, скилла или инструкции агента («поправь правило», «измени скилл», «запиши урок инцидента») — это тоже изменение поведения, и роль красного теста там играет евал-кейс в evals/. НЕ для вёрстки/скриновых тестов (PP/testplane — свой цикл), конфигов, разовых скриптов и спайков-прототипов.
---

# Шим: тело — в Craft

Тело скилла живёт в Craft-доке «⚙️ SKILL: TDD-цикл»
(block-ID `3685d305-b6e6-aee7-1b6a-e6d2d2fe6f63`). Прочитай его ЦЕЛИКОМ —
`blocks get 3685d305-b6e6-aee7-1b6a-e6d2d2fe6f63 --depth -1` через Craft MCP —
и действуй строго по нему.

Craft недоступен (ни MCP, ни connect) — ЯВНО скажи Владу, что тело скилла
недоступно, и не имитируй процедуру по памяти или усечённой версии.
