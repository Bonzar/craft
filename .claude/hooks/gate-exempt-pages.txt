# Предодобренные зоны прямого редактирования — страницы Craft, запись ВНУТРЬ
# которых не требует плана (план-гейт пропускает craft_write, если ВСЕ block-ID
# команды лежат под одной из этих страниц). Это общая механика послабления гейта,
# не хардкод под конкретный список: новая зона = добавить сюда её page-ID.
#
# Зачем по цели записи, а не по словам в сообщении Влада: асимметрия у этого
# послабления ОБРАТНАЯ детектору инцидентов. Ложно открыть гейт для реальной
# записи в проект/сферу — ДОРОГО; лишний план на предодобренной операции —
# ДЁШЕВО. Фраза («купил X») ненадёжна — Влад покупает и кольца, и билеты, а не
# только продукты; надёжен только целевой блок. Поэтому склоняйся к УЗКОМУ
# списку: сюда идёт лишь страница-список с низкими ставками и собственной
# обработкой неоднозначности (переспрос у Влада внутри своего SKILL).
#
# Формат: по одному page-ID на строку; строки с '#' и пустые игнорируются.
# ID — корневой block-ID страницы из Craft. Кэш поддеревьев собирает
# cache-gate-exempt-scope.sh на SessionStart → .claude/craft-gate-exempt-scope.txt.

# «Продукты» — список покупок (SKILL: Продукты). Операции: пометить купленным /
# нужным / отменённым, добавить товар. Своя обработка неоднозначности в скилле.
395450FC-468E-4EF6-8267-BC158A4E2EBC
