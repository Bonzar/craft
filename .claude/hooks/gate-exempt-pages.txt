# Предодобренные зоны прямого редактирования Craft: страницы, запись ВНУТРЬ
# которых не требует план-мода (план-гейт пропускает craft_write, когда ВСЕ
# block-ID команды лежат под одной из них). Общая механика, не хардкод: новая
# зона = добавить сюда её page-ID.
#
# Держи список УЗКИМ. Асимметрия обратная детектору инцидентов: ложно открыть
# гейт для записи в проект/сферу — дорого, лишний план на предодобренной
# операции — дёшево. Сюда идут только страницы-списки с низкими ставками и
# собственной обработкой неоднозначности в своём SKILL-доке.
#
# Формат: один page-ID (корневой block-ID страницы) на строку;
# '#'-комментарии и пустые строки игнорируются. Кэш поддеревьев собирает
# craft-cache-gate-exempt-scope.sh на SessionStart
# → .claude/craft-gate-exempt-scope.txt (gitignored).

# «Продукты» — список покупок (SKILL: Продукты): пометить купленным/нужным,
# добавить/убрать товар. Неоднозначность разбирает сам скилл.
395450FC-468E-4EF6-8267-BC158A4E2EBC
